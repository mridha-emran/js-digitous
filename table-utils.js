function multiply(number){
    
    for(var x=1;x<=10;x++){
    

            console.log(number + " * " + (x) + " = " + x*number );


    }
}
function addition(number){
    
    for(var x=1;x<=10;x++){
    

            console.log(number + " * " + (x) + " = " + x*number );


    }
}

module.exports = {
    multiply,
    addition
}